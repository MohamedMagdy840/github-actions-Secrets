name: Using Secrets

on: workflow_dispatch

jobs:
  show-secret:
    runs-on: ubuntu-latest
    steps:
      - name: Use the secret safely
        env:
          MY_SECRET: ${{ secrets.MY_SECRET }}
        run: echo "$MY_SECRET"
